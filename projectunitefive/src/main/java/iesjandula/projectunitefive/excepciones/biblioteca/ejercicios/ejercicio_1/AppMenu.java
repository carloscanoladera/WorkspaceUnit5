package iesjandula.projectunit5.excepciones.ejercicios.ejercicio_1;

import java.util.Scanner;

public class AppMenu {

	public static void main(String[] args) {
		
		// Metodos del bloc de notas 
		/*
		 * 
		 * constructores
		 * 
		 * getNotas();
		 * addNota(Nota nota);
		 * eliminarNota(Nota nota);
		 * addNotas(Nota[] notasAdd);  // esto espera un array de notas.
		 * hayNotas()
		 * buscarNotaPorId(int id)  -> devuelve nota
		 * listarNotas()
		 * ordenarNotas();
		 * ordenarNotas(Comparator<Nota> comp) -> pasar lamba con criterio de comparator.
		 * devuelvaNotasOrdenado() -> devuelve una copia de array ordenado
		 * devuelvaNotasOrdenado(Comparator<Nota> comp) -> Con comparator.
		 */
		
		
		
	
		

	}

}
